services:
  - type: web
    name: audio-converter
    runtime: docker
    dockerfilePath: ./Dockerfile
    plan: free
    rootDir: converter
    envVars:
      - key: PORT
        value: 3001
      - key: FRONTEND_URL
        value: https://vidvert.netlify.app
    healthCheckPath: /health
    autoDeploy: true
